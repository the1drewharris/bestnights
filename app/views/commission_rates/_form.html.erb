<%= form_for @commission_rate do |f| %>
  <ul>
  	<li>
  		<%= f.label 'Rate' %>
  		<%= f.text_field(:amount, class: "textfield") %> %
  	</li>
  </ul>
<div class="form-actions">
  <%= f.button :submit %>
</div>
<% end %>

